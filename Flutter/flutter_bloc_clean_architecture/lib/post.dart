/// Post model. This is what we would call domain model
/// in Clean Architecture, but here it is used for
/// everything. In the full CA example, consider this as
/// DomainPost.
typedef Post = ({int id, String title, String body});
